# =============================================================================
# Odin's Almanac - Environment Variables Example
# =============================================================================
# Viking Restaurant Consultants LLC
# 
# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in all required values with your actual credentials
# 3. NEVER commit .env file to version control (it's in .gitignore)
# 4. Keep credentials secure and rotate them regularly
# =============================================================================

# -----------------------------------------------------------------------------
# Stripe Configuration (Required for payment processing)
# -----------------------------------------------------------------------------

# Stripe Publishable Key (starts with pk_test_ for test mode, pk_live_ for production)
# Where to find: Stripe Dashboard → Developers → API Keys
STRIPE_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Stripe Secret Key (starts with sk_test_ for test mode, sk_live_ for production)
# Where to find: Stripe Dashboard → Developers → API Keys
# ⚠️ CRITICAL: Keep this secret! Never share or commit to version control
STRIPE_SECRET_KEY=sk_test_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# -----------------------------------------------------------------------------
# Database Configuration (Required)
# -----------------------------------------------------------------------------

# PostgreSQL Database Connection String
# Format: postgresql://username:password@host:port/database?sslmode=require
# Example: postgresql://user:pass@ep-cool-darkness-123456.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=postgresql://user:pass@host:5432/db

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------

# Node.js environment (development, production, or test)
NODE_ENV=production

# Application port (default: 3000, Azure uses 3001 or sets via PORT)
PORT=3001

# Node.js version for Azure App Service (use ~20 for Node 20.x)
WEBSITE_NODE_DEFAULT_VERSION=~20

# -----------------------------------------------------------------------------
# Azure Application Insights (Optional but recommended for production)
# -----------------------------------------------------------------------------

# Application Insights connection string for monitoring and telemetry
# Where to find: Azure Portal → Application Insights → Overview → Connection String
# Leave empty to disable Application Insights
APPLICATIONINSIGHTS_CONNECTION_STRING=

# -----------------------------------------------------------------------------
# GitHub Integration (Optional)
# -----------------------------------------------------------------------------

# GitHub Personal Access Token for repository access and operations
# Where to find: GitHub → Settings → Developer settings → Personal access tokens
# Scopes needed: repo (for private repos), public_repo (for public repos)
GITHUB_TOKEN=

# -----------------------------------------------------------------------------
# AI Service Configuration (Optional - for AI-powered features)
# -----------------------------------------------------------------------------

# OpenAI API Key (for GPT models)
# Where to find: OpenAI Platform → API Keys
OPENAI_API_KEY=

# Anthropic API Key (for Claude models)
# Where to find: Anthropic Console → API Keys
ANTHROPIC_API_KEY=

# -----------------------------------------------------------------------------
# Additional Configuration (Optional)
# -----------------------------------------------------------------------------

# Frontend URL for CORS (if using separate frontend deployment)
# FRONTEND_URL=https://your-frontend.azurewebsites.net

# Azure App URL (your deployed app URL)
# AZURE_APP_URL=https://odinsalmanac.azurewebsites.net

# Session secret for Express sessions (generate with: openssl rand -base64 32)
# SESSION_SECRET=your-secure-random-string-here

# Enable debug logging (for troubleshooting)
# DEBUG=express:*

# -----------------------------------------------------------------------------
# Security Best Practices
# -----------------------------------------------------------------------------
#
# 1. ✅ Never commit .env or real credentials to version control
# 2. ✅ Use different credentials for development, staging, and production
# 3. ✅ Rotate secrets regularly (every 90 days recommended)
# 4. ✅ Use Azure Key Vault for production secrets (recommended)
# 5. ✅ Use Stripe test keys (pk_test_/sk_test_) for development
# 6. ✅ Enable database SSL/TLS connections (sslmode=require)
# 7. ✅ Use strong, unique passwords (minimum 16 characters)
# 8. ✅ Monitor access logs and set up alerts for unusual activity
#
# -----------------------------------------------------------------------------
# Getting Your Credentials
# -----------------------------------------------------------------------------
#
# Stripe Keys:
# 1. Sign up at https://stripe.com
# 2. Go to Developers → API Keys
# 3. Use test keys for development, live keys for production
#
# Database (Neon - Recommended, Free tier available):
# 1. Sign up at https://neon.tech
# 2. Create a new project
# 3. Copy the connection string from the dashboard
#
# For help, refer to DEPLOYMENT-GUIDE.md or README.md
# =============================================================================
