# =============================================================================
# Odin's Almanac Server Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control - it contains sensitive information

# Server Configuration
NODE_ENV=development
PORT=8080
APP_BASE_URL=http://localhost:8080

# Stripe Payment Configuration
# Get these from your Stripe Dashboard: https://dashboard.stripe.com/
STRIPE_SECRET_KEY=sk_test_REPLACE_WITH_YOUR_STRIPE_SECRET_KEY
STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_WITH_YOUR_STRIPE_PUBLISHABLE_KEY

# Stripe Price IDs (create these in Stripe Dashboard > Products)
STRIPE_STARTER_PRICE_ID=price_REPLACE_WITH_STARTER_PLAN_PRICE_ID
STRIPE_PRO_PRICE_ID=price_REPLACE_WITH_PRO_PLAN_PRICE_ID
STRIPE_PLATINUM_PRICE_ID=price_REPLACE_WITH_PLATINUM_PLAN_PRICE_ID
STRIPE_ENTERPRISE_PRICE_ID=price_REPLACE_WITH_ENTERPRISE_PLAN_PRICE_ID

# Optional: Webhook Configuration
# STRIPE_WEBHOOK_SECRET=whsec_REPLACE_WITH_YOUR_WEBHOOK_SECRET

# =============================================================================
# Production Environment Variables
# =============================================================================
# For production deployment, set NODE_ENV=production and use these:

# Production Stripe Keys (live mode)
# STRIPE_SECRET_KEY=sk_live_YOUR_LIVE_SECRET_KEY
# STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_LIVE_PUBLISHABLE_KEY

# Production URLs
# APP_BASE_URL=https://your-domain.com

# =============================================================================
# Setup Instructions
# =============================================================================
# 1. Create a Stripe account at https://stripe.com
# 2. Get your API keys from https://dashboard.stripe.com/test/apikeys
# 3. Create products and pricing in https://dashboard.stripe.com/test/products
# 4. Copy the price IDs to the variables above
# 5. For production, switch to live mode and get live keys